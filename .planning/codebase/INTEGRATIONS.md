# External Integrations

**Analysis Date:** 2026-04-01

## APIs & External Services

**Maps:**
- Google Maps (embed/redirect only)
  - Usage: Contact page "Open in Maps" button links to Google Maps with encoded address
  - Implementation: `src/pages/ContactPage.jsx` line 129
  - No SDK integration — client-side URL deeplink only

**Video Hosting:**
- Pexels (free stock video CDN)
  - Usage: Hero section, CTA section, testimonials background videos
  - Files: `src/data/videoSources.js`
  - URLs: Direct CDN links (e.g., `https://videos.pexels.com/video-files/...`)
  - Configuration: Dual quality options (SD 640x360 and HD 1920x1080)
  - Note: Royalty-free, no attribution required

**Font Hosting:**
- Google Fonts
  - Families: Plus Jakarta Sans, DM Sans, JetBrains Mono
  - Implementation: `@import url()` in `src/index.css` line 1
  - Delivery: CSS @import (loads fonts on page init)

## Data Storage

**Databases:**
- None — stateless SPA

**File Storage:**
- Local filesystem only
- Public assets in `public/` directory (images, videos can be placed here)
- No cloud storage service integrated (S3, GCS, Cloudinary, etc.)

**Caching:**
- Browser caching only
- Vite handles static asset versioning in production build

## Authentication & Identity

**Auth Provider:**
- None — public website, no user authentication

**Form Submission:**
- Contact Form: `src/pages/ContactPage.jsx` (lines 58-107)
  - Status: TODO stub (line 69)
  - Currently shows alert only, no backend integration
  - Fields: name, email, phone, company, message

- Quote Request Form: `src/pages/QuotePage.jsx` (lines 22-130)
  - Status: TODO stub (line 42)
  - Currently shows alert only, no backend integration
  - Fields: name, email, phone, company, serviceType, material, quantity, timeline, file upload, description
  - File upload UI ready but not processed

## Monitoring & Observability

**Error Tracking:**
- Not detected

**Logs:**
- Browser console only (no error reporting service)
- No Sentry, LogRocket, or similar integration

**Analytics:**
- Not detected
- No Google Analytics, Mixpanel, or tracking pixels

## CI/CD & Deployment

**Hosting:**
- Not specified (codebase is static, compatible with any static host)
- Build output: `dist/` directory (generated by `vite build`)
- Recommended: Vercel, Netlify, GitHub Pages, AWS S3 + CloudFront

**CI Pipeline:**
- Not detected (no GitHub Actions, GitLab CI, etc. configured)

**Build Commands:**
- `npm run dev` — Local development with HMR
- `npm run build` — Production build to `dist/`
- `npm run preview` — Preview production build locally
- `npm run lint` — Run ESLint on `.js` and `.jsx` files

## Environment Configuration

**Required env vars:**
- None — all configuration is hardcoded

**Static Configuration:**
- `src/data/siteData.js` — Company data (COMPANY object)
  - Company name, phone, email, address, hours, certifications
  - Navigation links, statistics, service descriptions, industry categories
- `src/data/videoSources.js` — Video CDN URLs

**Secrets location:**
- None present
- When backend is added:
  - Contact form endpoint (email service or API)
  - Quote form endpoint (file upload destination, email service)
  - Optional: Recaptcha key for form protection

## Webhooks & Callbacks

**Incoming:**
- None — static frontend

**Outgoing:**
- None currently
- Future: Contact form POST to backend API
- Future: Quote form POST with file upload

## Form Submission Flow

**Contact Form:**
1. User fills form in `src/pages/ContactPage.jsx`
2. handleSubmit prevents default (line 68)
3. Currently shows alert (line 70) — **TODO: backend integration**
4. No actual email or API call happens

**Quote Form:**
1. User fills form and optionally uploads CAD file in `src/pages/QuotePage.jsx`
2. File name captured in `fileName` state (line 28-37)
3. handleSubmit prevents default (line 41)
4. Currently shows alert (line 43) — **TODO: backend integration**
5. No file is actually uploaded or processed

## Recommended Integrations for Production

**Email Service:**
- Resend, SendGrid, Mailgun, or AWS SES
- Use for: Contact form, Quote request notifications

**File Upload:**
- Cloudinary, AWS S3, or Supabase Storage
- Use for: CAD drawing uploads (STEP, IGS, PDF up to 50MB)

**Error Handling:**
- Sentry or LogRocket for production monitoring

**Analytics:**
- Google Analytics 4 or Plausible for traffic insights

**Form Validation & Protection:**
- hCaptcha or Google reCAPTCHA v3 for spam prevention

---

*Integration audit: 2026-04-01*
