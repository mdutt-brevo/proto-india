---
phase: 05-integration
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified: []
autonomous: false
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04
must_haves:
  truths:
    - "Live hero shows injection molding animation on the right side of the layout"
    - "Hero text reveals animate in smoothly on mount (no flash of invisible text)"
    - "Services, Industries, WhyChooseUs sections animate cards in on scroll"
    - "Both hero CTAs are clickable and navigate to /quote and /gallery"
    - "No Three.js canvas or GearLoader canvas appears anywhere on the page"
    - "GearLoader SVG spinner appears during page-load Suspense"
  artifacts:
    - path: "src/components/home/Hero.jsx"
      provides: "Integrated hero with InjectionMoldingLoop"
    - path: "src/components/home/ServicesSection.jsx"
      provides: "Motion whileInView section"
    - path: "src/components/home/IndustriesSection.jsx"
      provides: "Motion whileInView section"
    - path: "src/components/home/WhyChooseUs.jsx"
      provides: "Motion whileInView section"
  key_links:
    - from: "Hero.jsx right column"
      to: "InjectionMoldingLoop animation"
      via: "visual presence on screen"
      pattern: "visual"
---

<objective>
Human verification checkpoint — confirm the full integration works correctly in
the browser before the phase is marked complete.

Purpose: Plans 01 and 02 automated all code changes. This plan is the visual
gate: a human verifies the animation appears, CTAs work, and sections scroll-animate.
Output: Signed-off integration. Phase 5 complete.
</objective>

<execution_context>
@/Users/mayank.dutta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mayank.dutta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-integration/05-01-SUMMARY.md
@.planning/phases/05-integration/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and verify App.jsx INT-03</name>
  <files>src/App.jsx</files>
  <action>
Start the Vite dev server:

```
npm run dev
```

While it starts, verify INT-03 (App.jsx GearLoader) programmatically:

```bash
grep -n "GearLoader\|Three\|three\|canvas" src/App.jsx
```

Expected: only `GearLoader` import and `PageLoader` function. No Three.js references.
If any Three.js reference found, flag it before the human verify step.
  </action>
  <verify>
    <automated>cd /Users/mayank.dutta/projects/protolabs-india && grep -c "three\|Three\|@react-three" src/App.jsx && echo "THREE FOUND — FAIL" || echo "APP CLEAN — PASS"</automated>
  </verify>
  <done>
    Dev server running. App.jsx confirmed Three.js-free. INT-03 satisfied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify full integration in browser</name>
  <files></files>
  <action>
Pause automated execution. Present the verification checklist to the user and wait
for their confirmation. The dev server started in Task 1 is running at
http://localhost:5173.
  </action>
  <verify>
    <automated>MISSING — this task requires human visual confirmation; no automated substitute exists.</automated>
  </verify>
  <done>User types "approved" confirming all 8 visual checks passed.</done>
  <what-built>
    Plan 01: Hero.jsx — InjectionMoldingLoop wired into right column, all text
    reveals upgraded to Motion initial/animate, stats bar upgraded to Motion whileInView.

    Plan 02: ServicesSection, IndustriesSection, WhyChooseUs — useInView replaced
    with Motion whileInView card reveal variants.

    App.jsx (Phase 1, INT-03): GearLoader SVG spinner — already in place, verified
    in Task 1.
  </what-built>
  <how-to-verify>
    Open http://localhost:5173 in the browser.

    1. HERO ANIMATION (INT-01)
       On a desktop viewport (1024px+): Is there an injection molding animation
       playing on the right side of the hero? It should loop through 7 scenes
       (granules → melting → injection → filling → cooling → ejection → product reveal).

    2. HERO TEXT REVEALS (INT-04)
       Hard-refresh the page (Cmd+Shift+R). Do the badge, headline, body text,
       capability tags, and CTA buttons animate in smoothly from below on load?
       (They should fade+slide up in sequence, NOT flash-appear instantly.)

    3. HERO CTAs (INT-04 constraint)
       Click "Get a Quote" — does it navigate to /quote?
       Click "View Our Work" — does it navigate to /gallery?
       Right-click each button and inspect — confirm id="hero-get-quote-cta"
       and id="hero-view-work-cta" are present.

    4. STATS BAR (INT-04)
       Scroll down slightly so the stats bar enters view. Do the 4 stat numbers
       animate in with a stamp-drop effect (brief overshoot)?

    5. SERVICES SECTION (INT-02)
       Scroll to Services section. Do the 6 service cards scale in one-by-one
       as the section enters the viewport?

    6. INDUSTRIES SECTION (INT-02)
       Scroll to Industries section. Do the industry cards stamp in from above?

    7. WHY CHOOSE US SECTION (INT-02)
       Scroll to Why Choose Us. Do the 3 cards stamp in with staggered delay?

    8. GEAR LOADER (INT-03)
       Navigate to /about (or any page), then navigate back. If you see a
       loading spinner briefly, it should be the CSS gear SVG — no black canvas.

    9. REDUCED MOTION (optional)
       In macOS System Settings → Accessibility → Display, enable "Reduce motion".
       Reload. The hero should show the static InjectionMoldingStatic fallback SVG
       instead of the animated loop.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 8 checks pass.
    Or describe any issues (e.g., "animation not showing", "CTA broken", "section cards not animating").
  </resume-signal>
</task>

</tasks>

<verification>
Phase 5 is complete when the human verify checkpoint returns "approved":
- INT-01: InjectionMoldingLoop visible in hero ✓
- INT-02: Section whileInView animations working ✓
- INT-03: No Three.js anywhere in running app ✓
- INT-04: Hero text/CTA reveals preserved, CTA IDs intact ✓
</verification>

<success_criteria>
- Human has confirmed all 8 visual checks in the browser
- Phase 5 Integration is marked complete in ROADMAP.md and STATE.md
- The site ships with the new Motion animation system end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration/05-03-SUMMARY.md`

Also update:
- `.planning/ROADMAP.md`: Mark Phase 5 as complete, update plan checkboxes
- `.planning/STATE.md`: Update `status`, `stopped_at`, `completed_phases`
- `.planning/REQUIREMENTS.md`: Mark INT-01, INT-02, INT-03, INT-04 as [x]
</output>
