---
phase: 03-scenes-1-3
plan: 04
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - src/components/animation/InjectionMoldingLoop.jsx
autonomous: false
requirements:
  - HER-10
must_haves:
  truths:
    - "InjectionMoldingLoop renders GranulesScene for sceneIndex 0, MeltingScene for index 1, InjectionScene for index 2"
    - "SceneStubs at indices 3-6 are preserved — only indices 0, 1, 2 are replaced"
    - "Existing SCENE_LABELS, STUB_COLORS, AnimatePresence mode='wait', and SceneWrapper structure are unchanged"
    - "The loop advances through all 7 scenes and loops back to 0 without errors"
    - "The scenes directory is imported at the correct relative path from InjectionMoldingLoop.jsx"
  artifacts:
    - path: "src/components/animation/InjectionMoldingLoop.jsx"
      provides: "Orchestrator with real scene components at indices 0-2"
      contains: "GranulesScene, MeltingScene, InjectionScene imports"
    - path: "src/components/animation/scenes/GranulesScene.jsx"
      provides: "Already exists from plan 03-01"
    - path: "src/components/animation/scenes/MeltingScene.jsx"
      provides: "Already exists from plan 03-02"
    - path: "src/components/animation/scenes/InjectionScene.jsx"
      provides: "Already exists from plan 03-03"
  key_links:
    - from: "InjectionMoldingLoop.jsx"
      to: "scenes/GranulesScene.jsx"
      via: "import GranulesScene from './scenes/GranulesScene'"
      pattern: "import.*GranulesScene"
    - from: "InjectionMoldingLoop.jsx"
      to: "scenes/MeltingScene.jsx"
      via: "import MeltingScene from './scenes/MeltingScene'"
      pattern: "import.*MeltingScene"
    - from: "InjectionMoldingLoop.jsx"
      to: "scenes/InjectionScene.jsx"
      via: "import InjectionScene from './scenes/InjectionScene'"
      pattern: "import.*InjectionScene"
---

<objective>
Wire GranulesScene, MeltingScene, and InjectionScene into InjectionMoldingLoop.jsx
by replacing SceneStub at indices 0, 1, and 2.

Purpose: Connect the three Phase 3 scene components to the live orchestrator so the
animation loop renders real SVG scenes for the first half of the cycle. Scenes 4-7
remain as SceneStub placeholders for Phase 4.

Output: Updated InjectionMoldingLoop.jsx — imports 3 scene components, replaces
SceneStub for indices 0-2, preserves all existing orchestrator structure.
</objective>

<execution_context>
@/Users/mayank.dutta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mayank.dutta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scenes-1-3/03-CONTEXT.md

@src/components/animation/InjectionMoldingLoop.jsx
@src/lib/motionTokens.js

@.planning/phases/03-scenes-1-3/03-01-SUMMARY.md
@.planning/phases/03-scenes-1-3/03-02-SUMMARY.md
@.planning/phases/03-scenes-1-3/03-03-SUMMARY.md
</context>

<interfaces>
<!-- Current InjectionMoldingLoop.jsx structure (Phase 2 state) -->

```jsx
// Current render logic (to be updated):
<SceneWrapper key={sceneIndex}>
  <SceneStub index={sceneIndex} />
</SceneWrapper>

// SceneStub reads an index and renders a colored div placeholder.
// Phase 3 replaces SceneStub at indices 0, 1, 2 with real components.
// SceneStub at indices 3-6 must be PRESERVED.
```

<!-- Scene components contract (from plans 01-03) -->
```jsx
// All three scene components:
// - Export a default function component
// - Accept NO props
// - Contain their own m.svg with initial="hidden" animate="visible"
// - Are self-contained — no external state needed
import GranulesScene  from "./scenes/GranulesScene";   // index 0
import MeltingScene   from "./scenes/MeltingScene";     // index 1
import InjectionScene from "./scenes/InjectionScene";  // index 2
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace SceneStub indices 0-2 in InjectionMoldingLoop.jsx</name>
  <files>src/components/animation/InjectionMoldingLoop.jsx</files>

  <read_first>
    - src/components/animation/InjectionMoldingLoop.jsx — current full file (Phase 2 state)
    - src/components/animation/scenes/GranulesScene.jsx — confirm default export name
    - src/components/animation/scenes/MeltingScene.jsx — confirm default export name
    - src/components/animation/scenes/InjectionScene.jsx — confirm default export name
  </read_first>

  <action>
Read src/components/animation/InjectionMoldingLoop.jsx in full before editing.

**Changes to make — minimal and surgical:**

1. Add three import statements after the existing imports (after the InjectionMoldingStatic import):
```jsx
import GranulesScene  from "./scenes/GranulesScene";
import MeltingScene   from "./scenes/MeltingScene";
import InjectionScene from "./scenes/InjectionScene";
```

2. Replace the `SceneStub` render inside `InjectionMoldingLoop` with a lookup
   that returns real components for indices 0-2 and falls back to `SceneStub` for 3-6:

Replace this render section:
```jsx
<SceneWrapper key={sceneIndex}>
  <SceneStub index={sceneIndex} />
</SceneWrapper>
```

With:
```jsx
<SceneWrapper key={sceneIndex}>
  {sceneIndex === 0 && <GranulesScene />}
  {sceneIndex === 1 && <MeltingScene />}
  {sceneIndex === 2 && <InjectionScene />}
  {sceneIndex >= 3  && <SceneStub index={sceneIndex} />}
</SceneWrapper>
```

**Do NOT change anything else:**
- SCENE_LABELS array — preserve exactly
- STUB_COLORS array — preserve exactly (still needed for indices 3-6)
- SceneStub function — preserve exactly (still renders indices 3-6)
- AnimatePresence mode="wait" — preserve
- aria-live, role="img", className — preserve
- useMoldingLoop import and usage — preserve
- All comments — preserve

**Why conditional JSX over a SCENES array lookup:**
A SCENES array pattern (`const SCENES = [GranulesScene, MeltingScene, ...]`) is
cleaner long-term, but requires 7 complete entries — Phase 4 scenes do not exist yet.
Conditional JSX clearly shows which indices are real vs stub without requiring a
complete array. Phase 5 (full wiring) will replace this with an array.

**Verify import paths:**
The scene files are at `src/components/animation/scenes/GranulesScene.jsx`.
InjectionMoldingLoop.jsx is at `src/components/animation/InjectionMoldingLoop.jsx`.
Relative path from the orchestrator to scenes: `"./scenes/GranulesScene"` (no extension needed).

**After editing, read the full file back** to confirm no accidental deletions or
merge artifacts before moving on.
  </action>

  <verify>
    <automated>cd /Users/mayank.dutta/projects/protolabs-india && npx vite build --mode development 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - InjectionMoldingLoop.jsx imports GranulesScene, MeltingScene, InjectionScene from "./scenes/*"
    - SceneStub is rendered only for sceneIndex >= 3 (indices 3, 4, 5, 6)
    - GranulesScene renders for sceneIndex === 0
    - MeltingScene renders for sceneIndex === 1
    - InjectionScene renders for sceneIndex === 2
    - SCENE_LABELS, STUB_COLORS, SceneStub function, AnimatePresence config all unchanged
    - No duplicate or conflicting render paths for any index
    - `npx vite build` completes with zero errors
    - `grep -n "GranulesScene\|MeltingScene\|InjectionScene" src/components/animation/InjectionMoldingLoop.jsx` shows 6 lines (3 imports + 3 conditionals)
  </acceptance_criteria>

  <done>
InjectionMoldingLoop.jsx wires scenes 1-3 with real components. SceneStubs preserved
for indices 3-6. Build passes clean. Loop plays real SVG scenes for first 4.5 seconds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify scenes 1-3 animate correctly in browser</name>
  <files>src/components/animation/InjectionMoldingLoop.jsx</files>
  <action>
No code changes in this task. Start the dev server with `npm run dev` and open
http://localhost:5173 to visually verify the three animated scenes play correctly.
The loop should show Scene 1 (granule fall), Scene 2 (heat sweep), Scene 3 (injection
stroke), then stub scenes 4-7 before looping back. Approve or describe issues.
  </action>
  <what-built>
Three animated SVG scenes (GranulesScene, MeltingScene, InjectionScene) wired
into the live InjectionMoldingLoop orchestrator. The loop now shows real animations
for the first 3 scenes (4.5s) then falls back to stub colored divs for scenes 4-7.
  </what-built>

  <how-to-verify>
1. Start dev server: `npm run dev`
2. Open http://localhost:5173 in a browser
3. Navigate to the hero section where InjectionMoldingLoop renders
4. Watch the first 4.5 seconds of the loop:
   - Scene 1 (0-1.5s): Slate-colored rounded squares should fall into a hopper shape
   - Scene 2 (1.5-3s): Barrel shape with heat sweep + granule compression
   - Scene 3 (3-4.5s): Thick orange line drawing from nozzle to mould (pathLength)
   - Scene 4+ (4.5s+): Falls back to stub colored divs (expected for Phase 3)
5. Watch the full 12s loop cycle at least once to confirm it loops back to Scene 1
6. Open browser DevTools → Performance tab → record 15 seconds of animation
   → verify no frames consistently exceed 16ms
7. Optional: In DevTools, set `navigator.hardwareConcurrency` mock or check
   on a mobile device — particle count should be visibly fewer on mobile
  </how-to-verify>

  <verify>
    <automated>cd /Users/mayank.dutta/projects/protolabs-india && npx vite build --mode development 2>&1 | grep -c "built in" || echo "build check failed"</automated>
  </verify>

  <acceptance_criteria>
    - Scene 1 shows slate rounded-square particles falling into a hopper shape
    - Scene 2 shows barrel with progressive orange heat sweep and granule compression
    - Scene 3 shows thick orange pathLength stroke from nozzle to mould
    - Loop continues through scenes 4-7 stubs then returns to Scene 1 seamlessly
    - No console errors during playback
  </acceptance_criteria>

  <done>
Human confirms scenes 1-3 are visually correct and the full 12s loop plays without errors.
  </done>

  <resume-signal>
Type "approved" if scenes 1-3 animate correctly and the loop continues through scenes 4-7 stubs.
Describe any visual issues (wrong colors, missing shapes, no animation, console errors) if not approved.
  </resume-signal>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx vite build` passes clean — zero errors
2. `grep "GranulesScene\|MeltingScene\|InjectionScene" src/components/animation/InjectionMoldingLoop.jsx` returns 6 lines
3. `grep "SceneStub" src/components/animation/InjectionMoldingLoop.jsx` still returns lines (stub preserved for 3-6)
4. Human visual verification of scenes 1-3 animation quality
</verification>

<success_criteria>
- InjectionMoldingLoop.jsx updated with 3 new imports and 4 conditional render branches
- Scenes 1-3 render real SVG animations in the live browser loop
- Scenes 4-7 continue rendering SceneStub colored divs (Phase 4 work)
- No console errors during 12-second loop playback
- Human approves visual appearance of scenes 1-3
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-scenes-1-3/03-04-SUMMARY.md`
</output>
