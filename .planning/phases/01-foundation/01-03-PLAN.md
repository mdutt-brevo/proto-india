---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/App.jsx
  - src/lib/motionTokens.js
autonomous: true
requirements:
  - INF-01
  - INF-02

must_haves:
  truths:
    - "LazyMotion with domAnimation wraps BrowserRouter at the app level"
    - "All future motion components can use the m alias instead of motion"
    - "motionTokens.js exports SCENE_DURATIONS array summing to 12.0 seconds"
    - "motionTokens.js exports easing curves, transition presets, and COLOR constants"
    - "App builds with no LazyMotion import errors"
  artifacts:
    - path: "src/App.jsx"
      provides: "LazyMotion provider wrapping BrowserRouter"
      contains: "LazyMotion"
    - path: "src/lib/motionTokens.js"
      provides: "Shared animation constants for Phase 2-4 scene components"
      exports:
        - SCENE_DURATIONS
        - EASE_OUT_EXPO
        - EASE_IN_OUT_SINE
        - EASE_SPRING_DEFAULT
        - SCENE_ENTER
        - SCENE_EXIT
        - MICRO
        - REVEAL
        - COLOR
  key_links:
    - from: "src/App.jsx"
      to: "motion/react LazyMotion"
      via: "import { LazyMotion, domAnimation } from motion/react"
      pattern: "LazyMotion.*features.*domAnimation"
    - from: "src/lib/motionTokens.js SCENE_DURATIONS"
      to: "Phase 2 useMoldingLoop hook"
      via: "import { SCENE_DURATIONS } from ../lib/motionTokens"
      pattern: "export const SCENE_DURATIONS"
---

<objective>
Add LazyMotion provider to App.jsx and create the motionTokens.js constants file.
LazyMotion wraps BrowserRouter so all downstream m.* components pay only 4.6KB
(not 34KB) on the initial load. motionTokens.js is the single source of truth for
all animation constants — Phase 2+ scene components import from here instead of
hardcoding values.

Purpose: Establishes the animation infrastructure contract. LazyMotion is the gate
— without it, Phase 2-4 components cannot use the lightweight m alias.
motionTokens.js is referenced by useMoldingLoop (Phase 2) from day one.

Output: Updated App.jsx with LazyMotion wrapper. New src/lib/motionTokens.js file
with 9 named exports covering scene timing, easings, transition presets, and brand
color constants.
</objective>

<execution_context>
@/Users/mayank.dutta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mayank.dutta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Verified exports from motion/react (v12.38.0 installed in node_modules) -->

Available from "motion/react" (all verified present):
- LazyMotion   — component, accepts features prop
- domAnimation — feature bundle (4.6KB vs 34KB for full motion import)
- m            — lightweight motion element factory (m.div, m.span, m.path, etc.)
- useReducedMotion — hook for OS reduced-motion preference (used in Phase 2)

LazyMotion contract:
  LazyMotion MUST be an ancestor of any component using m.* elements.
  features={domAnimation} loads the complete DOM animation feature set lazily.

m alias rule for all Phase 2+ components:
  import { m } from "motion/react";
  Use m.div, m.span, m.path — NEVER motion.div when LazyMotion is present.
  Importing { motion } directly bypasses LazyMotion and pays the full bundle cost.

App.jsx state after Plan 01 completes (what executor will find):
  - DarkModeProvider is removed
  - GearLoader3D is removed, GearLoader SVG is the direct import
  - BrowserRouter is the outermost JSX element in App()
  - lazy and Suspense imports remain (still used for page components)

After this plan, App() will be:
  return (
    <LazyMotion features={domAnimation}>
      <BrowserRouter>
        <AppLayout />
      </BrowserRouter>
    </LazyMotion>
  );
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add LazyMotion provider to App.jsx</name>
  <read_first>
    - /Users/mayank.dutta/projects/protolabs-india/src/App.jsx
  </read_first>
  <files>src/App.jsx</files>
  <action>
    Edit src/App.jsx with exactly two changes:

    1. Add import at the top of the file (after react-router-dom and react imports,
       before Navbar/Footer imports):
         import { LazyMotion, domAnimation } from "motion/react";

    2. In the App() return statement, wrap BrowserRouter with LazyMotion:
         export default function App() {
           return (
             <LazyMotion features={domAnimation}>
               <BrowserRouter>
                 <AppLayout />
               </BrowserRouter>
             </LazyMotion>
           );
         }

    Do not modify AppLayout, PageLoader, ScrollToTop, or any other part of the file.
    The lazy and Suspense imports remain unchanged (page components still use them).

    Verify the motion package resolves:
      ls /Users/mayank.dutta/projects/protolabs-india/node_modules/motion/dist/ | head -3
    Should list files (motion is installed).
  </action>
  <verify>
    <automated>grep -n "LazyMotion\|domAnimation" /Users/mayank.dutta/projects/protolabs-india/src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "LazyMotion" src/App.jsx returns two matches (import line and JSX usage)
    - grep "domAnimation" src/App.jsx returns two matches (import and features prop)
    - grep "features={domAnimation}" src/App.jsx matches exactly once
    - grep "from \"motion/react\"" src/App.jsx matches exactly one import line
    - npm run build exits with code 0 after this change
  </acceptance_criteria>
  <done>
    App.jsx wraps the entire app tree with LazyMotion + domAnimation. All future m.*
    components in the tree use the lazy-loaded 4.6KB animation bundle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/lib/motionTokens.js with animation constants</name>
  <read_first>
    - /Users/mayank.dutta/projects/protolabs-india/src/App.jsx (confirm Plan 01 and Task 1 above are done)
  </read_first>
  <files>src/lib/motionTokens.js</files>
  <action>
    Create the directory if it does not exist:
      mkdir -p /Users/mayank.dutta/projects/protolabs-india/src/lib

    Create src/lib/motionTokens.js with the content below. Write every line exactly
    as shown — these values are load-bearing contracts for Phase 2 useMoldingLoop:

    ---
    // src/lib/motionTokens.js
    // Single source of truth for all animation constants used across Phases 2-4.
    // Scene components import from here — never hardcode animation values inline.

    // ── Scene timing ──────────────────────────────────────────────────────────────
    // Total: 12.0 seconds (within the 10-14s INF-02 / HER-09 requirement).
    // useMoldingLoop in Phase 2 reads this array to advance scene index.
    export const SCENE_DURATIONS = [
      1.5,  // Scene 1: Raw Material — granule particles falling into hopper
      1.5,  // Scene 2: Melting — granules through barrel with heat color shift
      1.5,  // Scene 3: Injection — pathLength stroke through nozzle into mould
      2.0,  // Scene 4: Mould Filling — clipPath cavity reveal
      2.0,  // Scene 5: Cooling — glow reduction, mould halves separating
      1.5,  // Scene 6: Ejection — ejector pins pushing finished part out
      2.0,  // Scene 7: Product Reveal — final part + "Precision in Every Moulded Part"
    ];
    // Sum: 12.0s

    // ── Easing curves (cubic-bezier arrays) ───────────────────────────────────────
    export const EASE_OUT_EXPO    = [0.16, 1, 0.3, 1];
    export const EASE_IN_OUT_SINE = [0.37, 0, 0.63, 1];

    // ── Spring preset ─────────────────────────────────────────────────────────────
    export const EASE_SPRING_DEFAULT = { type: "spring", stiffness: 300, damping: 30 };

    // ── Transition presets (use as Motion transition prop) ────────────────────────
    export const SCENE_ENTER = { duration: 0.4, ease: EASE_OUT_EXPO };
    export const SCENE_EXIT  = { duration: 0.3, ease: EASE_IN_OUT_SINE };
    export const MICRO       = { duration: 0.15 };
    export const REVEAL      = { duration: 0.6, ease: EASE_OUT_EXPO };

    // ── Brand color constants ──────────────────────────────────────────────────────
    // These mirror tailwind.config.js values. Motion animate() calls do not go
    // through Tailwind JIT, so hex values must be explicit here.
    export const COLOR = {
      baseBg:         "#0f1923",
      surfacePrimary: "#64748b",
      accentOrange:   "#ea580c",
      textPrimary:    "#f1f5f9",
      textMuted:      "#94a3b8",
    };
    ---

    After writing the file, do a quick smoke check:
      node --input-type=module -e "
        import { SCENE_DURATIONS } from
          '/Users/mayank.dutta/projects/protolabs-india/src/lib/motionTokens.js';
        const sum = SCENE_DURATIONS.reduce((a,b)=>a+b,0);
        console.log('Sum:', sum, sum === 12 ? 'PASS' : 'FAIL');
      "
    Expected output: Sum: 12 PASS
  </action>
  <verify>
    <automated>grep -c "export const" /Users/mayank.dutta/projects/protolabs-india/src/lib/motionTokens.js</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/motionTokens.js exists
    - grep "export const SCENE_DURATIONS" src/lib/motionTokens.js matches
    - grep "export const COLOR" src/lib/motionTokens.js matches
    - grep "export const SCENE_ENTER\|export const SCENE_EXIT" src/lib/motionTokens.js matches both
    - grep -c "export const" src/lib/motionTokens.js returns 9 (nine named exports)
    - grep "64748b" src/lib/motionTokens.js matches the surfacePrimary COLOR value
    - grep "ea580c" src/lib/motionTokens.js matches the accentOrange COLOR value
    - npm run build exits with code 0 (build still passes after new file is added)
  </acceptance_criteria>
  <done>
    src/lib/motionTokens.js exists with 9 named exports. SCENE_DURATIONS has 7
    entries summing to 12.0 seconds. Phase 2 useMoldingLoop can import from this
    file immediately.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

  # LazyMotion in App.jsx
  grep -n "LazyMotion\|domAnimation\|features" /Users/mayank.dutta/projects/protolabs-india/src/App.jsx

  # motionTokens.js exports
  grep "^export" /Users/mayank.dutta/projects/protolabs-india/src/lib/motionTokens.js

  # Build passes with all Phase 1 changes combined
  cd /Users/mayank.dutta/projects/protolabs-india && npm run build && echo "BUILD PASSED"
</verification>

<success_criteria>
- INF-01: LazyMotion with features={domAnimation} wraps BrowserRouter in App.jsx
- INF-02: src/lib/motionTokens.js exists with SCENE_DURATIONS (7 elements, sum 12.0s),
  easing curves, transition presets, and COLOR constants — 9 named exports total
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation/01-03-SUMMARY.md with:
- Confirmation that LazyMotion is the outermost wrapper in App.jsx
- List of all 9 exports from motionTokens.js
- SCENE_DURATIONS sum (must be 12.0)
- Build status
</output>
