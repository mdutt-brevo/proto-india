---
phase: 04-scenes-4-7-and-polish
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/components/animation/InjectionMoldingLoop.jsx
autonomous: false
requirements: [PRF-02, PRF-04, RES-01, RES-03, RES-04, RES-05]

must_haves:
  truths:
    - "Scenes 4-7 replace SceneStubs at indices 3-6 in InjectionMoldingLoop"
    - "The loop advances through all 7 scenes and returns to Scene 1 without a frame pop"
    - "User can watch the full loop in-browser and confirm the loop seam is seamless"
    - "All 7 scene SVGs use viewBox=0 0 400 300 — no overflow at any breakpoint"
    - "prefers-reduced-motion returns InjectionMoldingStatic (existing behavior, unchanged)"
  artifacts:
    - path: "src/components/animation/InjectionMoldingLoop.jsx"
      provides: "Orchestrator wired with all 7 scene components"
      contains: "FillingScene, CoolingScene, EjectionScene, ProductRevealScene"
  key_links:
    - from: "InjectionMoldingLoop.jsx"
      to: "FillingScene, CoolingScene, EjectionScene, ProductRevealScene"
      via: "conditional JSX at sceneIndex 3-6"
      pattern: "sceneIndex === [3-6]"
---

<objective>
Wire FillingScene, CoolingScene, EjectionScene, and ProductRevealScene into the InjectionMoldingLoop orchestrator (replacing SceneStubs at indices 3-6). Then run a full human-verified loop watch to confirm the seamless 7-scene cycle.

Purpose: The four new scenes become part of the live animation loop; the loop seam (Scene 7→Scene 1) is confirmed by human eyes in-browser.
Output: InjectionMoldingLoop.jsx with all 7 scenes wired; verified full-loop animation.
</objective>

<execution_context>
@/Users/mayank.dutta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mayank.dutta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-scenes-4-7-and-polish/04-CONTEXT.md
@.planning/phases/04-scenes-4-7-and-polish/04-01-SUMMARY.md
@.planning/phases/04-scenes-4-7-and-polish/04-02-SUMMARY.md

<interfaces>
<!-- Current InjectionMoldingLoop.jsx structure that executor will modify. -->

From src/components/animation/InjectionMoldingLoop.jsx (current state after Phase 3):
```jsx
import { AnimatePresence } from "motion/react";
import { useMoldingLoop } from "../../hooks/useMoldingLoop";
import SceneWrapper from "./SceneWrapper";
import InjectionMoldingStatic from "./InjectionMoldingStatic";
import GranulesScene  from "./scenes/GranulesScene";
import MeltingScene   from "./scenes/MeltingScene";
import InjectionScene from "./scenes/InjectionScene";

// ...SCENE_LABELS, STUB_COLORS, SceneStub (keep all of these)...

export default function InjectionMoldingLoop() {
  const { sceneIndex, shouldReduceMotion } = useMoldingLoop();
  if (shouldReduceMotion) return <InjectionMoldingStatic />;
  return (
    <div className="relative w-full aspect-video overflow-hidden" role="img" ...>
      <span className="sr-only" aria-live="polite" aria-atomic="true">
        {SCENE_LABELS[sceneIndex]}
      </span>
      <AnimatePresence mode="wait">
        <SceneWrapper key={sceneIndex}>
          {sceneIndex === 0 && <GranulesScene />}
          {sceneIndex === 1 && <MeltingScene />}
          {sceneIndex === 2 && <InjectionScene />}
          {sceneIndex >= 3  && <SceneStub index={sceneIndex} />}
        </SceneWrapper>
      </AnimatePresence>
    </div>
  );
}
```

Wire pattern to follow (from Phase 3 wiring):
- Add 4 new imports after existing scene imports
- Replace `{sceneIndex >= 3 && <SceneStub index={sceneIndex} />}` with 4 conditional JSX lines
- SceneStub function and STUB_COLORS array can be removed once all stubs are replaced
  (but keep SCENE_LABELS — still needed for aria-live)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire scenes 4-7 into InjectionMoldingLoop</name>
  <files>src/components/animation/InjectionMoldingLoop.jsx</files>
  <action>
Read the current InjectionMoldingLoop.jsx in full before editing.

Make the following targeted changes:

1. ADD four import statements after the existing scene imports (after InjectionScene import):
   ```js
   import FillingScene       from "./scenes/FillingScene";
   import CoolingScene       from "./scenes/CoolingScene";
   import EjectionScene      from "./scenes/EjectionScene";
   import ProductRevealScene from "./scenes/ProductRevealScene";
   ```

2. REPLACE the final catch-all render branch:
   ```jsx
   {sceneIndex >= 3  && <SceneStub index={sceneIndex} />}
   ```
   WITH four explicit conditionals:
   ```jsx
   {sceneIndex === 3 && <FillingScene />}
   {sceneIndex === 4 && <CoolingScene />}
   {sceneIndex === 5 && <EjectionScene />}
   {sceneIndex === 6 && <ProductRevealScene />}
   ```

3. REMOVE SceneStub function and STUB_COLORS const (they are no longer needed — all stubs replaced).
   Keep the SCENE_LABELS array — it is used by the aria-live span.

4. Update the top file comment to read:
   "// Phase 4: all 7 scene components wired. SceneStub removed."

Do NOT change: AnimatePresence, SceneWrapper, key={sceneIndex}, SCENE_LABELS, useMoldingLoop, shouldReduceMotion branch, role/aria attributes, sr-only span.

After editing, run: `npx vite build --mode development` to confirm zero errors.
  </action>
  <verify>
    <automated>cd /Users/mayank.dutta/projects/protolabs-india && npx vite build --mode development 2>&1 | tail -8</automated>
  </verify>
  <done>
InjectionMoldingLoop.jsx imports all 7 scene components, conditional JSX for all 7 indices present, SceneStub removed, Vite build clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — full 7-scene loop in browser</name>
  <what-built>
Full 7-scene injection molding loop wired: GranulesScene, MeltingScene, InjectionScene,
FillingScene, CoolingScene, EjectionScene, ProductRevealScene — all advancing in sequence
and looping back to Scene 1.
  </what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev` (or it may already be running)
2. Open http://localhost:5173 in your browser
3. Navigate to the hero section where InjectionMoldingLoop is rendered
4. Watch the full loop — 12 seconds, 7 scenes:
   - Scene 1: Orange granule particles fall into hopper
   - Scene 2: Barrel heat sweep (orange glow through barrel)
   - Scene 3: Orange injection stroke draws from nozzle to mould
   - Scene 4: Orange fill rising upward inside mould cavity (clipPath)
   - Scene 5: Orange-to-slate color shift on part, then mould halves pull apart
   - Scene 6: Ejector pins + part translate upward out of mould base
   - Scene 7: Finished housing scales in, tagline "Precision in Every Moulded Part" fades in
5. Watch Scene 7 fade to Scene 1 — confirm NO frame pop or flash at the seam
6. Let the loop run at least twice to confirm it is stable

Additional checks:
- Resize window to ~375px — confirm no horizontal overflow from SVG
- If you have an iPhone or reduce-motion OS setting available, confirm InjectionMoldingStatic renders (static SVG) instead of the loop
  </how-to-verify>
  <resume-signal>
Type "approved" if all 7 scenes animate correctly and the loop seam is seamless.
Describe any specific issues if something looks wrong (e.g., "Scene 5 — mould halves don't move" or "frame pop at Scene 7→1 boundary").
  </resume-signal>
  <action>Pause for human visual verification. All automation complete — this task awaits user confirmation.</action>
  <verify><automated>echo "checkpoint — awaiting human approval"</automated></verify>
  <done>User types "approved" confirming all 7 scenes animate and loop seam is seamless.</done>
</task>

</tasks>

<verification>
- `grep -c "sceneIndex ===" src/components/animation/InjectionMoldingLoop.jsx` returns 7 (one per scene)
- `grep "SceneStub" src/components/animation/InjectionMoldingLoop.jsx` returns zero matches
- `grep "FillingScene\|CoolingScene\|EjectionScene\|ProductRevealScene" src/components/animation/InjectionMoldingLoop.jsx` returns 8 lines (4 imports + 4 JSX uses)
- Vite build: zero errors
</verification>

<success_criteria>
InjectionMoldingLoop.jsx wires all 7 scene components, SceneStub is deleted, Vite build is clean.
Human confirms: all 7 scenes animate as designed, loop seam is seamless (no pop at Scene 7→1), SVG does not overflow at 375px viewport.
</success_criteria>

<output>
After completion, create `.planning/phases/04-scenes-4-7-and-polish/04-03-SUMMARY.md`
</output>

